import { css } from "lit";

export const styleModule = css`
  .filters {
    display: flex;
    justify-content: center;
    gap: 1rem;
    margin: 1rem 0;
    flex-wrap: wrap;
  }

  select {
    padding: 0.5rem;
    background-color: #00cfe8;
    border: none;
    color: white;
  }
`;
